#include "Log.h" // can also be used here 
/*
 * Header files are used to list available functions.
 * A common place for only declarations
 * 
 * 
 */

// hi!
int main(int argc, char **argv)
{
	//InitLog();
	int a = 10;
	Log("hello world");
	return 0;
}
